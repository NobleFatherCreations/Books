# Real screenshots go here

Any file you drop in this folder **silently replaces** the hand-drawn SVG
mockup of the same name. No code change needed — redeploy and it upgrades.

Filenames must match exactly. See `../../SCREENSHOT_SHOTLIST.md` for the
full list, what should be on screen, and capture instructions.

Already shipped (real captures from Shae's phone):
- `nfc-tools-home.png`
- `write-tab.png`
- `app-store-ios.png`
