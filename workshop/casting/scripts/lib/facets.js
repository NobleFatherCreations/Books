/* The facet vocabulary is used identically by the build scripts and by
   the gallery in the browser, so it lives in one file under assets/ and
   is re-exported here. Keeping two copies in sync by hand is exactly how
   a taxonomy quietly diverges. */
export * from '../../assets/js/facets.js';
